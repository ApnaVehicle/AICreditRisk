# Database Configuration
# SQLite database file path (relative to prisma/ directory)
DATABASE_URL="file:./dev.db"

# OpenAI API Configuration (REQUIRED)
# Get your API key from: https://platform.openai.com/api-keys
# Used for AI agent functionality with tool calling support
OPENAI_API_KEY="your_openai_api_key_here"

# Optional: Choose your preferred model (default: gpt-4-turbo)
# Available models:
# - gpt-4-turbo: Best balance of cost and performance ($10/$30 per million tokens)
# - gpt-4o: Faster responses, lower cost ($2.50/$10 per million tokens)
# - gpt-4: Most capable, higher cost ($30/$60 per million tokens)
OPENAI_MODEL="gpt-4-turbo"

# Application Configuration
NODE_ENV="development"

# Optional: Application Port (default: 3000)
# PORT=3000

# Optional: API Configuration
# API_TIMEOUT=30000
# MAX_REQUESTS_PER_MINUTE=60
